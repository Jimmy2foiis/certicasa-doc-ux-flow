
// Create a mock interface to maintain compatibility
interface ComponentProps {
  // Add any required props here
}

const DocumentTemplateUpload = (props: ComponentProps) => {
  return (
    <div className="p-4 border rounded-md">
      <p>Cette fonctionnalité nécessite une intégration Supabase.</p>
    </div>
  );
};

export default DocumentTemplateUpload;
