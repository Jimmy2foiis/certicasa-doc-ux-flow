// Real data placeholders - no more mock data
// All data now comes from real APIs

export const recentProjects = []; // Will be loaded from API
export const clientsData = []; // Will be loaded from Beetool/internal API
export const clientDocuments = []; // Will be loaded from API
export const documentTypes = []; // Will be loaded from API
