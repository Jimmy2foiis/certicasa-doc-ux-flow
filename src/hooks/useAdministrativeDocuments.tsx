
// This is now just a re-export of the refactored hook for backward compatibility
import { useClientDocuments } from "./documents/useClientDocuments";

export const useAdministrativeDocuments = useClientDocuments;
